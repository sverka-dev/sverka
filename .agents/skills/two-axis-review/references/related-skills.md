# Related skills

Skills this skill may delegate to or be used alongside:

- act — when the review findings are ready to be turned into per-thread fixes and resolved.
- adhd — goal focus when deciding whether to launch parallel sub-agents.
- architecture-review — concrete standard to feed the Standards sub-agent.
- performance-investigation — concrete standard to feed the Standards sub-agent.
- codehome — concrete standard to feed the Standards sub-agent.
- minimalist — concrete standard to feed the Standards sub-agent.
- external-tools — fetching issue/PR contents and spec sources from GitHub or GitLab.
- github-pr-review — single-axis, GitHub-aware review writer; use when you only need one axis or are posting back to a PR.
- shared-plan — when a review reveals a multi-step follow-up (refactor, ADR, or split-PR plan).
- subagent-capsule — orchestration rules for launching parallel sub-agents.
- token-rationalism — keeping the parallel sub-agent launch cost-effective.
