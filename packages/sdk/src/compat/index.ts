// Compatibility re-exports — delegate to the canonical SDK index.
// This file exists to preserve the import path `@sverka/sdk/compat`
// for downstream consumers during the v0 migration.

export * from "../index.js";
