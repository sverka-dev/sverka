{
  "name": "ir",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run tsdown",
        "cwd": "packages/ir"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run vitest run",
        "cwd": "packages/ir"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run oxlint src",
        "cwd": "packages/ir"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun run tsc --noEmit",
        "cwd": "packages/ir"
      }
    }
  }
}
