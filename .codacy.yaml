---
# Codacy configuration
# Excludes false positives that cannot be suppressed inline (Codacy does not
# support inline disable comments for TypeScript).
engines:
  eslint:
    exclude_paths:
      # Codacy false positive: flags `result[key] = sortKeysDeep(src[key])` as
      # "JSON stringify does not produce stable key ordering" — there is no
      # JSON.stringify call on this line. The function sorts object keys
      # recursively to produce deterministic output.
      - "packages/engine-native/src/engine.ts"
